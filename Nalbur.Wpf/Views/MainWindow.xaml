<Window x:Class="Nalbur.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewmodels="clr-namespace:Nalbur.Wpf.ViewModels"
        xmlns:views="clr-namespace:Nalbur.Wpf.Views"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="700" Width="1100"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:DashboardViewModel}">
            <views:DashboardView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ProductViewModel}">
            <views:ProductListView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:CustomerViewModel}">
            <views:CustomerListView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:SalesViewModel}">
            <views:SalesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:InstallmentViewModel}">
            <views:InstallmentTrackingView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Background="{DynamicResource MaterialDesign.Brush.Primary}" Grid.Column="0">
            <StackPanel Margin="10">
                <TextBlock Text="NALBUR" FontSize="24" FontWeight="Bold" Foreground="White" Margin="10,20,10,40" HorizontalAlignment="Center" />
                
                <Button Command="{Binding NavigateToDashboardCommand}" Style="{StaticResource MaterialDesignTextButton}" Foreground="White" HorizontalContentAlignment="Left" Height="50">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ViewDashboard" Width="24" Height="24" Margin="0,0,10,0"/>
                        <TextBlock Text="Dashboard" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding NavigateToProductsCommand}" Style="{StaticResource MaterialDesignTextButton}" Foreground="White" HorizontalContentAlignment="Left" Height="50">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="PackageVariant" Width="24" Height="24" Margin="0,0,10,0"/>
                        <TextBlock Text="Ürünler" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding NavigateToCustomersCommand}" Style="{StaticResource MaterialDesignTextButton}" Foreground="White" HorizontalContentAlignment="Left" Height="50">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountGroup" Width="24" Height="24" Margin="0,0,10,0"/>
                        <TextBlock Text="Müşteriler" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding NavigateToSalesCommand}" Style="{StaticResource MaterialDesignTextButton}" Foreground="White" HorizontalContentAlignment="Left" Height="50">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ShoppingCart" Width="24" Height="24" Margin="0,0,10,0"/>
                        <TextBlock Text="Satış Yap" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding NavigateToInstallmentsCommand}" Style="{StaticResource MaterialDesignTextButton}" Foreground="White" HorizontalContentAlignment="Left" Height="50">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CalendarClock" Width="24" Height="24" Margin="0,0,10,0"/>
                        <TextBlock Text="Taksit Takip" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" materialDesign:ElevationAssist.Elevation="Dp4">
                <TextBlock Text="{Binding Title}" Foreground="White" FontSize="18" FontWeight="SemiBold"/>
            </materialDesign:ColorZone>

            <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" Margin="16" />
        </Grid>
    </Grid>
</Window>
