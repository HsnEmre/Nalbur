<UserControl x:Class="Nalbur.Wpf.Views.CustomerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <!-- Customer List -->
        <materialDesign:Card Grid.Column="0" Margin="0,0,16,0" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="Müşteri Listesi" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16" />
                
                <DataGrid Grid.Row="1" ItemsSource="{Binding Customers}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ad" Binding="{Binding Name}" Width="100" />
                        <DataGridTextColumn Header="Soyad / Firma" Binding="{Binding SurnameCompany}" Width="*" />
                        <DataGridTextColumn Header="Telefon" Binding="{Binding Phone}" Width="120" />
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <!-- Add Form -->
        <materialDesign:Card Grid.Column="1" Padding="16">
            <StackPanel>
                <TextBlock Text="Yeni Müşteri Ekle" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16" />
                
                <TextBox Text="{Binding NewCustomer.Name}" materialDesign:HintAssist.Hint="Ad" Style="{StaticResource MaterialDesignTextBox}" Margin="0,0,0,16" />
                <TextBox Text="{Binding NewCustomer.SurnameCompany}" materialDesign:HintAssist.Hint="Soyad / Firma Adı" Style="{StaticResource MaterialDesignTextBox}" Margin="0,0,0,16" />
                <TextBox Text="{Binding NewCustomer.Phone}" materialDesign:HintAssist.Hint="Telefon" Style="{StaticResource MaterialDesignTextBox}" Margin="0,0,0,16" />
                <TextBox Text="{Binding NewCustomer.Email}" materialDesign:HintAssist.Hint="Email" Style="{StaticResource MaterialDesignTextBox}" Margin="0,0,0,16" />
                <TextBox Text="{Binding NewCustomer.Address}" materialDesign:HintAssist.Hint="Adres" AcceptsReturn="True" Height="80" VerticalAlignment="Top" Style="{StaticResource MaterialDesignTextBox}" Margin="0,0,0,16" />

                <Button Command="{Binding SaveCustomerCommand}" Content="KAYDET" Margin="0,20,0,0" Style="{StaticResource MaterialDesignRaisedButton}" />
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
