<UserControl x:Class="Nalbur.Wpf.Views.OutgoingPaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Margin="0,0,0,16" Padding="16">
            <StackPanel Orientation="Horizontal">
                <TextBox Width="180"
                         Margin="0,0,12,0"
                         Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Başlık" />

                <TextBox Width="140"
                         Margin="0,0,12,0"
                         Text="{Binding NewCategory, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Kategori" />

                <TextBox Width="120"
                         Margin="0,0,12,0"
                         Text="{Binding NewAmount, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Tutar" />

                <DatePicker Width="160"
                            Margin="0,0,12,0"
                            SelectedDate="{Binding NewDueDate}" />

                <Button Content="Ekle"
                        Command="{Binding AddPaymentCommand}" />
            </StackPanel>
        </materialDesign:Card>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Payments}"
                  SelectedItem="{Binding SelectedPayment}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Başlık" Binding="{Binding Title}" Width="*" />
                <DataGridTextColumn Header="Kategori" Binding="{Binding Category}" Width="160" />
                <DataGridTextColumn Header="Tutar" Binding="{Binding Amount, StringFormat={}{0:C}}" Width="120" />
                <DataGridTextColumn Header="Vade" Binding="{Binding DueDate, StringFormat=dd.MM.yyyy}" Width="120" />
                <DataGridCheckBoxColumn Header="Ödendi" Binding="{Binding IsPaid}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>